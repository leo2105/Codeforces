#include <bits/stdc++.h>
using namespace std;

struct nodo{
    int num, cant,
}
const int N = 1e5+5;

int Cont[N];

int main(){
    int n, a;
    map < int, int > M;

    for ( int i = 0 ; i < n ; i++ ) {
        cin >> a;   
        A.push_back(a);
    }

    sort( A.begin(), A.end() );

    for( int i = 0 ; i < n ; i++ ) {

    }
    return 0;
}
